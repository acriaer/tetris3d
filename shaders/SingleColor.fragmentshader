#version 330 core

in vec2 UV;
out vec3 color;

uniform sampler2D sampler;

void main()
{
	float color_val = 1;

	if (bool(int(int(UV.x * 100) % 10 < 5) ^ int(int(UV.y * 100) % 10 < 5)))
		color_val = 0;

	color = vec3(color_val, color_val, color_val);
}
